- name: loops
  hosts: web
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.package:
      name: {{ item.name }}
      state: present
    loops:
    - mysql
    - nginx
    - postfix
    - httpd